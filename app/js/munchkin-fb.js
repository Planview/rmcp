define(function () {
	window.Munchkin = {
		init: function () {},
		munchkinFunction: function () {},
		createTrackingCookie: function () {}
	};
	console.warn("Munchkin didn't load");
	return window.Munchkin;
});